# @amusite/vue-core

Vue 2/3 composables for tables, dictionaries, selections, modals, and async actions.
Vue 2 consumers must install and register `@vue/composition-api`.

```ts
import { useTable } from '@amusite/vue-core'
```
